install with:

```
mkdir sway-blue-dotfiles
cd sway-blue-dotfiles
git clone https://github.com/BrenoPT/sway-blue-dotfiles
sudo chmod +X arch_script.sh
./arch_script.sh
cd ..
rm -rf sway-blue-dotfiles
```
