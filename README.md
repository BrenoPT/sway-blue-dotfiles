install with:

```
mkdir sway-blue-dotfiles
cd sway-blue-dotfiles
git pull https://github.com/BrenoPT/sway-blue-dotfiles.git
sudo chmode +X ./arch_script.sh
./arch_script.sh
cd ..
rm -rf sway-blue-dotfiles
```
